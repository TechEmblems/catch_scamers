<%= form_for(@installment) do |f| %>
  <% if @installment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@installment.errors.count, "error") %> prohibited this installment from being saved:</h2>

      <ul>
      <% @installment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :purchase_id %><br>
    <%= f.number_field :purchase_id, :class=>'form-control' %>
  </div>
  <div class="field form-group">
    <%= f.label :amount %><br>
    <%= f.text_field :amount, :class=>'form-control' %>
  </div>
  <div class="field form-group">
    <%= f.label :paid_on %><br>
    <%= f.date_select :paid_on, :class=>'form-control' %>
  </div>
    <div class="actions form-group">
      <%= f.submit 'Save', :class=>'btn btn-success' %>
      <% if !@installment.new_record? %>
          <%= link_to 'Show', @installment, :class=>'btn btn-success' %>
      <% end %>
      <%= link_to 'Back', @installment_path, :class=>'btn btn-default' %>
    </div>
<% end %>
